<%= for u <- @users do %>
  <tr>
    <td><%= u.name %></td>
    <td><%= u.email %></td>
    <td></td>
    <td><%= Registro.User.role_label(u.role) %></td>
    <td><%= Registro.User.status_label(u.status) %></td>
    <td><%= branch_label(u) %></td>
  </tr>
<% end %>
